/**
 * vue中遍历全局的方法有哪些？
 * 1.普通遍历，对象.forEach
 *    arr.forEach((item,index,arr)=>{})
 *
 * 2.对元素统一操作 对象.map()
 *     var newArr = arr.map((item)=>{return item + 1})
 *
 * 3.查找符合条件的元素  对象.filter()
 *    arr.filter((item)=> item < 2)
 *
 */
