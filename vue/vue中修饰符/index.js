/**
 * 1.事件修饰符
 *    .stop           阻止冒泡
 *    .prevent        阻止默认行为
 *    .capture        内部元素触发的事件先在外部处理
 *    .self           只有在event.target是当前元素时触发
 *    .once           事件只会触发一次
 *    .passive        立即触发默认行为
 *    .native         把当前元素作为原生标签看待
 *
 *
 * 2.按键修饰符
 *    .keyup        键盘抬起
 *    .keydown      键盘按下
 *
 * 3.系统修饰符
 *    .ctrl
 *    .alt
 *    .meta
 *
 * 4.鼠标修饰符
 *    .left
 *    .right
 *    .middle
 *
 * 5.表单修饰符
 *    .lazy     等输入完之后再显示
 *    .trim     删除内容前后的空格
 *    .number   输入是数字或转为数字
 *
 *
 */
