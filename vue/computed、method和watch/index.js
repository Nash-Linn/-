/**
 * computed 、 method 、 watch 区别
 *
 * 一、computed 和 method
 *
 *      computed 有缓存
 *
 *      method 无缓存
 *
 *
 *
 * 二、computed 和 watch
 *
 *    watch是监听，数据或者路由发生了改变才可以响应
 *
 *    computed计算某一属性的改变，如果某一个值改变了，计算属性会监听到并进行返回
 *
 *    watch是当监听到数据改变了，才会执行内部代码
 *
 *
 */
